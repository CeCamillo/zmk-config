# Enable deep sleep when idle (saves battery if you later go wireless)
CONFIG_ZMK_SLEEP=y

# Combo & macro limits (raise if you add many)
CONFIG_ZMK_COMBO_MAX_COMBOS_PER_KEY=4
CONFIG_ZMK_COMBO_MAX_KEYS_PER_COMBO=4
